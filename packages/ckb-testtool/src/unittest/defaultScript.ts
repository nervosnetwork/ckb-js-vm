import * as path from "path";

export const DEFAULT_SCRIPT_ALWAYS_SUCCESS = path.join(
  __dirname,
  "../../src/unittest/defaultScript/alwaysSuccess",
);
export const DEFAULT_SCRIPT_ALWAYS_FAILURE = path.join(
  __dirname,
  "../../src/unittest/defaultScript/alwaysFailure",
);
